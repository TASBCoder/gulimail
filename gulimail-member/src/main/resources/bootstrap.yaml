spring:
  application:
    name: gulimail-member #服务名称
  profiles:
    active: dev #开发环境
  cloud:
    nacos:
      server-addr: localhost:8848 # Nacos配置中心地址
      config:
        file-extension: yaml #文件后缀名
        namespace: public #指定命名空间，默认是public
